# Issue tracker

Issues for this repo live as markdown files under `.scratch/<feature>/`.

Use the `.scratch/` directory for local issue tracking. Each feature or piece of work gets its own directory, containing the issue markdown and any supporting notes or artifacts.
