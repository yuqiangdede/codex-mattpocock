# 2026-09-04

- 使用 to-spec skill 将 `.scratch/mvp-implementation-readiness/` 下的 5 个序曲文档综合为 spec，发布到 `.scratch/m0-m1-implementation-readiness-spike/spec.md`，标签 `ready-for-agent`。
- Spec 覆盖 M0（协议与 Provider Spike）和 M1（纵向切片边界定义）两个阶段，测试 seam 为单一端到端 PowerShell 验证脚本。
- 确认了 Codex App Server 协议边界（stdio JSONL，排除 experimental API）、node:sqlite Packaged 验证、Provider Secret sentinel token 隔离验证和最小 NSIS/Portable 制品构建。
- 将 Spike Spec 拆成 17 个 Ticket 的 DAG（T-001 到 T-017），全部标签 `ready-for-agent`，发布到 `.scratch/m0-m1-implementation-readiness-spike/`。
- DAG 结构：T-001 脚手架是共同前置，Phase A（T-002~T-008,T-009,T-010 协议与 Provider）和 Phase B（T-011,T-012,T-013,T-014 Storage 与 Packaging）可并行，T-015 整合验证脚本依赖全部，T-016 Go/No-Go 决策，T-017 M1 边界定义。
- 五个停止条件：协议漂移、Tool/Streaming 缺失、凭据泄露、Packaged SQLite 不可用、断流后输入不确定。
- 使用 implement skill 完成 T-001 项目脚手架，commit c1efdf2。
- 脚手架包含：pnpm Workspaces + TypeScript Project References（10 个子项目）、electron-vite 构建 Main/Preload/Renderer、electron-builder 配置 NSIS+Portable target、三个标准脚本（init.ps1/start.ps1/verify.ps1）。
- 验证通过：pnpm install 成功、tsc --build 零错误、electron-vite build 产出三个 bundle。
- 技术决策：.npmrc 使用 node-linker=hoisted 解决 pnpm 在 Windows 上 @types 不可见问题；脚本兼容 PowerShell 5.1（release-gates.md 要求 PS7，M5 前升级）。
- 使用 code-review skill 对 T-001 做两轴审查（Standards + Spec），发现 4 个问题并修复（commit 86199e7）：删除 pnpm-workspace.yaml 无效 allowBuilds 块、main 字段从 .ts 改为 .js 输出路径、packages 源码降级为纯占位符、提取脚本公共逻辑到 common.ps1。
- 完成 T-002 Codex Binary 固定与验证（commit 5a25936）：固定 @openai/codex@0.153.2，复制 codex.exe 到 runtime/，计算 SHA256，生成 release-evidence/runtime-manifest.json，验证 binary 可启动，验证离线导入路径。
- 技术决策：GitHub 直连下载被网络阻断，通过 npm alias 安装 @openai/codex-win32-x64（npm:@openai/codex@0.153.2-win32-x64）从内部镜像获取 platform binary；codex app-server generate-ts 成功生成 90+ TypeScript Schema 文件。
- 完成 T-003 协议握手与 Schema 生成（commit 517a731）：完成 stdio JSONL initialize 握手，生成 94 个 TypeScript 类型 + 39 个 JSON Schema 文件，计算 Schema Hash，保存脱敏 JSONL Trace，记录 stable vs experimental API 边界。
- 完成 T-004 Thread lifecycle 协议验证（commit 5e62adb，用户手动提交）：验证 thread/start → thread/resume → thread/fork 生命周期。
- 完成 T-005 Turn control 协议验证（commit 72ef12b）：验证 turn/start（返回 Turn ID，status=inProgress）→ turn/steer（注入即时输入，返回匹配 turnId）→ turn/interrupt（中断成功，turn/completed 确认 status=interrupted）。发现 UserInput 格式是 {type:"text",text:"...",text_elements:[]} 而非 message/content wrapper。
- 完成 T-006 Approval 机制验证（commit 40563e1）：通过 Schema 验证四种 Approval 类型（Command/File/Permissions/MCP）的协议结构完整性，包括 server-initiated request 方法名、params 字段和 decision 响应类型。Turn 启动但未触发真实 Approval 请求（interrupt 在 model 响应前执行）。
- 应用户要求讲解了 Matt Pocock 方法论核心概念（Spec/Ticket/DAG/Spike）及相互关系，依据为 ~/.workbuddy/skills 下 11 个上游 skill（to-spec、to-tickets、grilling、prototype 等）与本仓库 CONTEXT.md、requirement-delivery-workflow.md、release-gates.md 和 .scratch 实际样例；附两张内联图（主流程 + Ticket DAG 样例）。
- 改为按上游 https://github.com/mattpocock/skills 核对术语：确认上游无 Spike/DAG 措辞，官方等价为 prototype 与 blocking edges；梳理了官方主流程、三条入口、词汇层与深模块词汇，术语归属结论已写入 MEMORY.md。
- 完成 T-007 Reviewer Subagent 验证（commit 9b4fa68）：成功启动两个并行 Reviewer（uncommittedChanges + custom instructions），各自返回不同 turn ID 可区分，Schema 验证 SubAgentSource=review、NonSteerableTurnKind=review、ReviewStarted/Completed notifications。
- 完成 T-008 断流不重发验证（commit e9c5b11）：SIGKILL 杀死 App Server 后重连，验证无自动重发（zero turn/started notifications）。thread/resume 因 rollout 文件为空而失败——架构发现：App Manager 必须自维护 Event Store 进行崩溃恢复，不能依赖 App Server rollout。停止条件 US-33 未触发。
- 完成 T-009 Provider Probe（commit 67bf947）：对 https://api.qnaigc.com/v1 + deepseek/deepseek-v4-flash-vision-exp 执行三项探测。Responses API (/v1/responses) 返回 404——提供商不支持；降级到 Chat Completions API (/v1/chat/completions) 三项全 PASS：API 兼容性 PASS、SSE Streaming PASS（text/event-stream, 105 chunks）、Tool Calling PASS（get_weather tool call 返回，arguments present）。停止条件 US-30 未触发。探测脚本设计为双端点降级模式（先试 Responses API，404 后自动降级到 Chat Completions）。报告不含 API Key。
- 完成 T-010 Probe 失效验证（commit ddb3572）：实现配置指纹机制（SHA-256 of base_url|model_id|credential_tag），凭据标签为 API Key 的截断 SHA-256。5 个测试用例全 PASS：无变化→VALID、base_url 变→INVALID、model_id 变→INVALID、凭据变→INVALID、三项全变→INVALID。
- 完成 T-011 Packaged SQLite 验证（commit 1a70844）：Electron 从 33.4.11 升级到 35.7.5（Node 20.18.3→22.16.0），因为 Electron 33/34 内嵌 Node 20 不支持 node:sqlite。8 项 SQLite 测试全 PASS：WAL、synchronous=FULL、foreign_keys=ON、trusted_schema=OFF、事务回滚、外键级联删除、并发读取不阻塞写入、busy_timeout。停止条件 US-32 未触发。
- 完成 T-012 Secret 隔离验证（commit 56a941b）：sentinel token 存放在私有配置目录，认证 Helper 读取后传递给 Codex Runtime。8 项隔离检查全 PASS：配置存在、Helper 提取成功、Runtime 可访问、Renderer 环境清洁、Project Shell 环境清洁、日志文件清洁、诊断包清洁、Utility Process allowlist 有效。停止条件 US-31 未触发。
- 完成 T-013 最小构建（commit cf04971）：NSIS Installer（82MB）和 Portable EXE（74MB）均成功生成。修复 electron-builder.json files 路径（apps/desktop/*/dist→out/）、package.json main 字段（out/main/index.js）、winCodeSign 缓存手动解压（macOS 符号链接在 Windows 无管理员权限失败）、NSIS 从 npmmirror 下载。签名正确跳过（延迟到 M5 Gate）。
- 完成 T-014 数据隔离验证（commit 91507e2）：6 项检查全 PASS——数据路径不同、NSIS 数据创建成功、Portable 数据创建成功、NSIS 标记不在 Portable 中、Portable 标记不在 NSIS 中、两种 target 均已配置。
- 完成 T-015 验证脚本整合（commit 678f07f）：verify.ps1 支持 --gate spike 参数，执行全部 12 项 Spike 验证检查（T-003 到 T-014），12/12 PASS，退出码 0。
- 完成 T-016 Go/No-Go 决策（commit c91c297）：15/15 Ticket 全 PASS，5 个停止条件全未触发。决策：**GO**——M1 产品实施可以开始。关键发现：Electron 35+ 必需、Chat Completions 降级、Agent Manager 需自维护 Event Store。
- 完成 T-017 M1 切片边界定义（commit 07b9b3e）：切片路径 Project Scan→Task Creation→Integration Worktree→Turn Execution→Approval→Diff→Verification→Renderer Reload Recovery。三栏 UI 布局、单事务持久化（Normalized Event + Projection + Attention Outbox）、最小 Fixture（hello() 函数 + 测试）、M2/M3 延后能力清单。
- **M0-M1 Spike 全部完成（T-001 到 T-017），17/17 Ticket PASS，Go 决策。**
